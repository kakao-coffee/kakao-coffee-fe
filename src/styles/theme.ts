export type themeTypes = {
  coffeeColor: string;
  backgroundColor: string;
  primaryColor: string;
  textColor: string;
};

export const theme: themeTypes = Object.freeze({
  coffeeColor: "#3C2A21",
  backgroundColor: "#1A120B",
  primaryColor: "#D5CEA3",
  textColor: "#FBF8F1",
});
